[build]
  base = "daily-brief-frontend"
  command = "npm install --platform=linux --arch=x64 lightningcss && npm run build"
  publish = ".next"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NODE_VERSION = "18"
  
[[plugins]]
  package = "@netlify/plugin-nextjs" 